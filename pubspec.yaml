name: serializer_libs_comparison
description: a comparison between different serializing packages
# version: 1.0.0

environment:
  sdk: ">=2.8.1 <3.0.0"

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4
  build_runner:
  hive_generator:
    git:
      url: git://github.com/hivedb/hive.git
      path: hive_generator

dependencies:
  flat_buffers: ^1.12.0
  hive: ^1.4.1+1
  msgpack_dart: ^0.0.7
  isar: ^0.0.0  # Notice copied binary from github!
